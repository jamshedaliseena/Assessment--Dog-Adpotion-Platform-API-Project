const mongoose = require('mongoose');
const dogSchema = new mongoose.Schema({ name: { type: String, required: true, trim: true, maxlength: 100 }, description: { type: String, required: true, trim: true, maxlength: 1000 }, owner: { type: mongoose.Schema.Types.ObjectId, ref: 'User', required: true }, adoptedBy: { type: mongoose.Schema.Types.ObjectId, ref: 'User', default: null }, thankYouMessage: { type: String, trim: true, maxlength: 1000, default: null }, adoptedAt: { type: Date, default: null } }, { timestamps: true });
dogSchema.index({ owner: 1, adoptedBy: 1 }); module.exports = mongoose.model('Dog', dogSchema);
